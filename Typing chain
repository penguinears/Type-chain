// File: src/GameHub.jsx

import React, { useState, useEffect, useRef } from 'react';
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { motion } from "framer-motion";

const wordBank = ["apple", "eagle", "ear", "rat", "tree", "elephant", "top", "pan", "note", "egg", "giraffe", "engine"];

function getRandomWord() {
  return wordBank[Math.floor(Math.random() * wordBank.length)];
}

const getLeaderboard = (game) => {
  const data = localStorage.getItem(`leaderboard_${game}`);
  return data ? JSON.parse(data) : [];
};

const saveToLeaderboard = (game, name, score) => {
  const existing = getLeaderboard(game);
  const updated = [...existing, { name, score }]
    .sort((a, b) => b.score - a.score)
    .slice(0, 10);
  localStorage.setItem(`leaderboard_${game}`, JSON.stringify(updated));
};

const Leaderboard = ({ game }) => {
  const [scores, setScores] = useState([]);

  useEffect(() => {
    setScores(getLeaderboard(game));
  }, [game]);

  return (
    <div className="mt-4">
      <h3 className="text-xl font-semibold mb-2">Leaderboard</h3>
      <ul className="list-disc list-inside">
        {scores.map((entry, index) => (
          <li key={index}>{index + 1}. {entry.name} - {entry.score}</li>
        ))}
      </ul>
    </div>
  );
};

const TypeChain = () => {
  const [chain, setChain] = useState([getRandomWord()]);
  const [input, setInput] = useState('');
  const [score, setScore] = useState(0);
  const [playerName, setPlayerName] = useState('');

  const handleSubmit = () => {
    const lastWord = chain[chain.length - 1];
    const lastLetter = lastWord[lastWord.length - 1];
    if (input.toLowerCase().startsWith(lastLetter) && !chain.includes(input)) {
      setChain([...chain, input]);
      setScore(score + 1);
      setInput('');
    }
  };

  const handleSave = () => {
    if (playerName) {
      saveToLeaderboard("typeChain", playerName, score);
      setScore(0);
      setChain([getRandomWord()]);
    }
  };

  return (
    <Card className="p-4 max-w-xl mx-auto mt-8 shadow-2xl rounded-2xl">
      <CardContent>
        <h2 className="text-2xl font-bold mb-4">Type Chain</h2>
        <p className="mb-4 text-lg">Last Word: <strong>{chain[chain.length - 1]}</strong></p>
        <Input value={input} onChange={(e) => setInput(e.target.value)} placeholder="Type next word..." />
        <Button className="mt-2" onClick={handleSubmit}>Submit</Button>
        <p className="mt-4 text-md">Score: {score}</p>
        <div className="flex gap-2 mt-2">
          <Input value={playerName} onChange={(e) => setPlayerName(e.target.value)} placeholder="Your name" />
          <Button onClick={handleSave}>Save Score</Button>
        </div>
        <Leaderboard game="typeChain" />
      </CardContent>
    </Card>
  );
};

const TypeNinja = () => {
  const [text, setText] = useState('');
  const [input, setInput] = useState('');
  const [score, setScore] = useState(0);
  const [startTime, setStartTime] = useState(null);
  const [playerName, setPlayerName] = useState('');

  useEffect(() => {
    setText(generateParagraph());
  }, []);

  const generateParagraph = () => {
    return Array.from({ length: 20 }, () => getRandomWord()).join(' ');
  };

  const handleTyping = (e) => {
    const value = e.target.value;
    if (!startTime) setStartTime(Date.now());
    setInput(value);

    if (value.trim() === text.trim()) {
      const timeTaken = (Date.now() - startTime) / 1000;
      const words = text.split(' ').length;
      const wpm = Math.round((words / timeTaken) * 60);
      setScore(wpm);
    }
  };

  const handleSave = () => {
    if (playerName) {
      saveToLeaderboard("typeNinja", playerName, score);
      setText(generateParagraph());
      setInput('');
      setStartTime(null);
    }
  };

  return (
    <Card className="p-4 max-w-xl mx-auto mt-8 shadow-2xl rounded-2xl">
      <CardContent>
        <h2 className="text-2xl font-bold mb-4">Type Ninja</h2>
        <p className="mb-4 text-lg">{text}</p>
        <textarea
          className="w-full p-2 border rounded-lg"
          rows={4}
          value={input}
          onChange={handleTyping}
          placeholder="Start typing..."
        />
        <p className="mt-4 text-md">WPM: {score}</p>
        <div className="flex gap-2 mt-2">
          <Input value={playerName} onChange={(e) => setPlayerName(e.target.value)} placeholder="Your name" />
          <Button onClick={handleSave}>Save Score</Button>
        </div>
        <Leaderboard game="typeNinja" />
      </CardContent>
    </Card>
  );
};

export default function GameHub() {
  return (
    <Tabs defaultValue="typeChain" className="w-full mt-8">
      <TabsList className="flex justify-center mb-4">
        <TabsTrigger value="typeChain">Type Chain</TabsTrigger>
        <TabsTrigger value="typeNinja">Type Ninja</TabsTrigger>
      </TabsList>
      <TabsContent value="typeChain">
        <TypeChain />
      </TabsContent>
      <TabsContent value="typeNinja">
        <TypeNinja />
      </TabsContent>
    </Tabs>
  );
}
